@model ArticleTag

<div class="card radius-15">
	<div class="card-body">
		<h5 class="card-title">@Model.Article.Title</h5>
		<h6 class="card-subtitle mb-2">@Model.Tag.Name</h6> 
	</div>
</div>

<h1>Silmek istediğinize emin misiniz?</h1>

<form action="/Management/ArticleTag/Delete/@Model.ArticleId/@Model.TagId"
method="post">
	@* <input type="hidden" asp-for="Id" value="@Model.Id" /> *@
	<input type="submit" value="Sil" class="btn btn-danger" />
</form>